router id 8.8.8.8;

log stderr all;

protocol device {
}

protocol direct {
    ipv4;
}

protocol kernel {
    ipv4 {
        import all;
        export all;
    };
}

protocol ospf ospf1 {

    area 20 {
        stub;

        interface "eth1" {
            hello 5;
        };

	interface "eth2" {
             stub;
        };

        interface "lo" {
            stub;
        };
    };
}
